#!/usr/bin/env bash
# Thin launcher: run the TypeScript CLI directly via tsx — no build step (v1).
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$DIR/../node_modules/.bin/tsx" "$DIR/../src/cli/index.ts" "$@"
